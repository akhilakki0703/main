open terminal
git init
git add .
git commit -m "update"
for 1st time: 
git config --global user.email "ur mail"
git config --global user.name "username"
do git commit -m "update" again


if you make change later ... again do git add . and git commit -m "update"

goto github.com create an account and repository.

login in vscode local using github,
when you do push it will open one login prompt,login there and give i agree authorization.

git remote add origin "link"
git push -u origin main/master